---
title: Bruxism CAP
created: 2026-05-03
updated: 2026-05-06
type: concept
tags: [dataset, evaluation, experiment, workflow, notes]
sources:
  - raw/articles/cap-sleep-database-physionet-2012.md
  - raw/articles/bruxism-single-channel-eeg-2024.md
  - raw/articles/sleep-bruxism-portable-emg-meta-analysis-2024.md
---

# Bruxism CAP

`bruxism-cap` is now the main active `ai-lab` project. It is a deliberately small public-data pilot for bruxism detection from EEG/EMG signals using the PhysioNet CAP Sleep Database. The goal is not a clinical claim or a deep model; the goal is to build a reproducible, leakage-aware baseline that can be extended later. As of `2026-05-04`, the project is now explicitly framed as an EMG-first benchmark: `EMG1-EMG2` is the primary next-pass channel and `C4-P4` is the comparison channel. [[bruxism-eeg-emg-starter-project-2026-05-03]] [[bruxism-cap-first-baseline-lessons-2026-05-03]] [[bruxism-cap-annotation-alignment-audit-2026-05-03]] [[ai-lab-learning-path]] [[artifact-card-sft]]

## Why this is the current focus
- It is grounded in open data that can be reproduced end to end.
- It is small enough to run locally on the VPS or MacBook without needing a GPU first.
- It teaches EDF loading, channel inspection, feature extraction, and subject-aware evaluation.
- It is a better immediate lab target than continuing another fine-tuning branch before a new non-text pipeline is running.

## Current scope
- Dataset: CAP Sleep Database subset beginning with `brux1`, `brux2`, and a few controls.
- Task: binary `bruxism` vs `control` classification.
- Modeling: classical handcrafted features plus simple baselines such as logistic regression, random forest, and SVM.
- Evaluation: compare random-window cross-validation against leave-one-subject-out cross-validation.

## Current repo status
- Scaffold exists under `projects/bruxism-cap/`.
- The first baseline report now records ten concrete stages:
  - a `3`-subject pilot (`pass1`)
  - the first valid `6`-subject baseline (`pass2`)
  - a first autoresearch-motivated rerun that excludes explicit sampling-rate proxy features from training (`pass3`)
  - a first annotation-aware rerun using `SLEEP-S2` windows from RemLogic sidecars on a stage-valid `5`-subject subset (`pass4`)
  - a subject-level LOSO hardening rerun (`pass5`)
  - a subject-versus-label confound audit (`pass6`)
  - a tighter event-aware rerun that keeps only `SLEEP-S2` windows overlapping CAP micro-events (`pass7`)
  - a narrower single-family rerun that keeps only `SLEEP-S2` windows overlapping `MCAP-A3` (`pass9`)
  - a stricter exclusive single-family rerun that keeps only `SLEEP-S2` windows with `MCAP-A3` overlap and excludes simultaneous `MCAP-A1/A2` overlap (`pass10`)
  - a compact rule-survival audit that tracks how much each overlap rule changes per-subject and per-label window availability before any model rerun (`pass11`)
- Current saved artifacts include:
  - `projects/bruxism-cap/data/window_features_pass1.csv`
  - `projects/bruxism-cap/data/window_features_pass2.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass1.json`
  - `projects/bruxism-cap/reports/random-window-cv-pass2.json`
  - `projects/bruxism-cap/reports/loso-cv-pass2.json`
  - `projects/bruxism-cap/reports/random-window-cv-pass3-nosfreq.json`
  - `projects/bruxism-cap/reports/loso-cv-pass3-nosfreq.json`
  - `projects/bruxism-cap/data/window_features_pass4_s2.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass4-s2.json`
  - `projects/bruxism-cap/reports/loso-cv-pass4-s2.json`
  - `projects/bruxism-cap/reports/annotation-alignment-audit-pass4-s2.md`
  - `projects/bruxism-cap/reports/subject-confound-audit-pass6.json`
  - `projects/bruxism-cap/reports/subject-confound-audit-pass6.md`
  - `projects/bruxism-cap/data/window_features_pass7_s2_mcap.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass7-s2-mcap.json`
  - `projects/bruxism-cap/reports/loso-cv-pass7-s2-mcap.json`
  - `projects/bruxism-cap/reports/pass7-s2-mcap-overlap.md`
  - `projects/bruxism-cap/data/window_features_pass9_s2_mcap_a3.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass9-s2-mcap-a3.json`
  - `projects/bruxism-cap/reports/loso-cv-pass9-s2-mcap-a3.json`
  - `projects/bruxism-cap/reports/pass9-s2-mcap-a3.md`
  - `projects/bruxism-cap/data/window_features_pass10_s2_mcap_a3_only.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass10-s2-mcap-a3-only.json`
  - `projects/bruxism-cap/reports/loso-cv-pass10-s2-mcap-a3-only.json`
  - `projects/bruxism-cap/reports/pass10-s2-mcap-a3-only.md`
  - `projects/bruxism-cap/reports/rule-survival-audit-pass11.json`
  - `projects/bruxism-cap/reports/pass11-rule-survival-audit.md`
  - `projects/bruxism-cap/data/window_features_pass12_s2_mcap_a1_only_matched14.csv`
  - `projects/bruxism-cap/data/window_features_pass12_s2_mcap_a3_only_matched14.csv`
  - `projects/bruxism-cap/reports/random-window-cv-pass12-s2-mcap-a1-only-matched14.json`
  - `projects/bruxism-cap/reports/loso-cv-pass12-s2-mcap-a1-only-matched14.json`
  - `projects/bruxism-cap/reports/random-window-cv-pass12-s2-mcap-a3-only-matched14.json`
  - `projects/bruxism-cap/reports/loso-cv-pass12-s2-mcap-a3-only-matched14.json`
  - `projects/bruxism-cap/reports/pass12-exclusive-a1-vs-a3-matched14.md`
  - `projects/bruxism-cap/src/audit_subject_thresholds.py`
  - `projects/bruxism-cap/reports/subject-threshold-audit-pass15-emg-a3-vs-c4-a1.json`
  - `projects/bruxism-cap/reports/pass15-emg-a3-threshold-audit.md`
  - `projects/bruxism-cap/src/audit_emg_feature_validity.py`
  - `projects/bruxism-cap/reports/feature-validity-audit-pass16-emg-a3-logreg.json`
  - `projects/bruxism-cap/reports/feature-validity-audit-pass16-emg-a3-logreg.md`
- The current result is useful but weak: random-window CV remains very high, while LOSO stays poor on held-out bruxism sensitivity.
- A pass5 measurement-hardening rerun added subject-level LOSO aggregation and made the current failure mode harsher: on both the pass3 and pass4 datasets, none of the tested models actually produced a positive held-out bruxism subject verdict at the default threshold.
- A follow-up alignment audit made the data-validity issue more specific:
  - `n10.edf` appears truncated relative to `n10.txt`, leaving no in-range `SLEEP-S2` windows
  - `brux1` has enough early `S2` windows to keep, but many later scored events also exceed the local EDF duration
  - `brux2`, `n3`, `n5`, and `n11` look clean enough for the current bounded `SLEEP-S2` extraction rule
- A new pass6 confound audit further refined the interpretation:
  - on both the pass3 and pass4 feature tables, label-separation metrics were stronger than subject-separation metrics inside leakage-prone random window splits
  - so the current failure is not best explained as trivial subject-ID memorization alone; the harder problem is that the observed class boundary does not survive held-out-subject transfer
- The new pass7 event-overlap rerun tightened the extraction rule again without improving transfer:
  - it kept only in-range `SLEEP-S2` windows that overlapped `MCAP-A1`, `MCAP-A2`, or `MCAP-A3`
  - random-window CV still stayed effectively perfect
  - LOSO window sensitivity fell to `0.000`, and subject-level bruxism sensitivity stayed `0.000`
  - so simple stage restriction plus CAP micro-event overlap is still not enough to make the boundary transferable
- A new pass8 overlap-event audit narrowed the next extraction question again:
  - `brux2` is heavily `MCAP-A3`-dominated while `n5` is heavily `MCAP-A1`-dominated within the kept pass7 windows
  - that mismatch already exists in the full eligible pools, not only in the first-20 cap
  - so the mixed `S2+MCAP` bucket is not one homogeneous physiological subset, and the next bounded rerun should try one single overlap family rather than another broad mixed-event bucket
- The new pass9 single-family rerun narrowed the overlap rule but still did not improve honest transfer:
  - random-window balanced accuracy fell from perfect to about `0.921`, which is directionally healthier than pass7
  - best LOSO balanced accuracy still fell to only `0.550`
  - subject-level bruxism sensitivity still stayed `0.000` for every model
  - so simply requiring `A3` overlap is not enough; the next extraction step should be even more auditable, such as exclusive `A3` windows rather than any window that merely contains `A3`
- The new pass10 exclusive-`A3` rerun made that next step explicit and still failed:
  - random-window balanced accuracy fell slightly further to about `0.908`
  - best LOSO balanced accuracy fell again to only `0.500`
  - subject-level bruxism sensitivity still stayed `0.000` for every model
  - so even exclusive `A3` windows do not isolate a transferable cross-subject boundary in the current tiny subset
- A new pass11 rule-survival audit clarified how much the overlap rules themselves change sampling availability:
  - the bruxism pool keeps `142/442` eligible pass4 `S2` windows (`32.1%`) under exclusive `A3`, while the control pool keeps only `156/1026` (`15.2%`)
  - `brux2` still has `111` eligible exclusive-`A3` windows, while `n5` falls to `38`
  - so pass7/pass9/pass10 should be read not only as semantic event-family changes but also as progressively less balanced sampling surfaces [[bruxism-cap-rule-survival-audit-2026-05-04]]
- A new pass12 matched-family comparison changed the overlap-family interpretation again:
  - when exclusive `A1-only` and exclusive `A3-only` are compared on the same verified 5-subject subset with the same `14` windows per subject, `A1-only` becomes the stronger honest transfer candidate
  - best `A1-only` LOSO balanced accuracy rose to `0.686`, and subject-level bruxism sensitivity rose to `0.500`
  - matched `A3-only` still stayed at `0.000` subject-level bruxism sensitivity across models
  - this is still not a stable baseline win, because `brux1` remains missed and the gain is carried by only one of the two bruxism subjects [[bruxism-cap-exclusive-a1-vs-a3-matched14-2026-05-04]]
- A new pass13 matched channel comparison added the first real EMG-first check on the same scaffold:
  - exclusive `A1-only` on `EMG1-EMG2` did **not** beat the existing matched `C4-P4` baseline on the same 5-subject / 14-window subset
  - best EMG LOSO balanced accuracy fell to only `0.543`, and subject-level bruxism sensitivity fell back to `0.000`
  - unlike the pass12 `C4-P4` run, the EMG rerun missed both `brux1` and `brux2`, so the first EMG-first comparison is a preserved negative result rather than a new honest baseline [[bruxism-cap-emg-vs-c4-a1-only-matched14-2026-05-04]]
- A new pass14 matched EMG-family comparison refined that EMG-first reading:
  - on the same matched `5`-subject / `14`-windows-per-subject scaffold, exclusive `A3-only` improved `EMG1-EMG2` LOSO balanced accuracy from `0.543` to `0.629` relative to exclusive `A1-only`
  - but subject-level bruxism sensitivity still stayed `0.000` for every tested model, so neither EMG family yet beats the honest baseline criterion
  - this is therefore a narrower validity result, not an EMG breakthrough: family choice matters inside `EMG1-EMG2`, but the current handcrafted EMG recipe still misses both held-out bruxism subjects [[bruxism-cap-emg-a1-vs-a3-matched14-2026-05-04]]
- A new pass15 subject-threshold audit tightened the EMG-first failure mode again:
  - the stronger `EMG1-EMG2 A3-only` run does **not** fail mainly because the subject threshold is too high
  - the best bruxism subject score (`brux1` `0.176`) still sits below two controls (`n3` `0.267`, `n5` `0.266`), so any threshold that recovers `brux1` also creates at least two control false positives
  - thresholds that recover both bruxism subjects collapse specificity entirely, so the next bottleneck is score ordering / feature validity rather than threshold choice [[bruxism-cap-emg-a3-threshold-audit-2026-05-04]]
- A new pass16 feature-validity audit tightened the EMG-first failure mode again:
  - the pass14 `EMG1-EMG2 A3-only` score ordering reproduces exactly under a fold-by-fold `logreg` rebuild: `n3` `0.267`, `n5` `0.266`, `brux1` `0.176`, `n11` `0.095`, `brux2` `0.074`
  - among the controls that still outrank `brux1`, the same small feature family keeps recurring as positive contributors: `ratio_alpha_delta`, `min`, and `sample_entropy`
  - `brux1` looks like a different instability surface because its fold is dominated by extreme absolute-power / mean terms, so the next EMG-first move should be one small feature-family ablation rather than more threshold tweaking [[bruxism-cap-emg-feature-validity-audit-2026-05-04]]
- A new pass17 time-domain ablation preserved another useful EMG-first negative result:
  - on the same matched `EMG1-EMG2 A3-only` scaffold, dropping `bp_*`, `rel_bp_*`, and `ratio_*` features does **not** improve the honest subject-level verdict; subject-level sensitivity stays `0.000`
  - the best LOSO window-level result also regresses slightly from `0.629` to `0.614`, and `n3` becomes even more dominant in the subject ranking (`0.328` vs `brux1` `0.148`)
  - so the next bounded patch should be replacement-oriented rather than subtraction-only: add one compact EMG-specific burst / envelope / amplitude-variability family instead of only deleting spectral features [[bruxism-cap-emg-time-only-ablation-2026-05-04]]
- A new pass18 replacement-family rerun preserved the same honest verdict while sharpening the next feature-selection question again:
  - on the same matched `EMG1-EMG2 A3-only` scaffold, adding one compact rectified-envelope / burst family does **not** improve the honest subject-level verdict; subject-level sensitivity stays `0.000`
  - the best LOSO window-level result regresses again from `0.614` to `0.600`, but the score ordering shifts in a useful mixed way: `brux1` rises from `0.148` to `0.158`, `brux2` from `0.055` to `0.092`, and `n3` falls from `0.328` to `0.245`
  - the remaining blocker is that `n5` still outranks `brux1` (`0.267` vs `0.158`), so add-only EMG features are not enough while the older spectral family still dominates the decision surface [[bruxism-cap-emg-envelope-replacement-2026-05-04]]

## Practical next step
- Keep the current model family fixed.
- Preserve the annotation-aware extraction path and treat EDF/sidecar alignment as part of the measurement pipeline.
- Keep `n10` excluded from local `SLEEP-S2` reruns unless a fuller matching EDF is found, and keep `brux1` only with explicit in-range filtering.
- Use the new subject-level LOSO summaries as the main interpretation surface for honest baseline quality.
- Keep the new event-overlap extraction support in `prepare_windows.py`, but treat pass7 as a negative result rather than a win.
- Preserve the new overlap-event audit as evidence that the mixed `S2+MCAP` bucket is heterogeneous by subject.
- Preserve the new rule-survival audit as evidence that the later `A3`-focused overlap filters also change per-subject and per-label availability.
- Preserve the new pass12 matched-family result as evidence that exclusive `A1-only` currently transfers better than matched exclusive `A3-only`, even though `brux1` still fails.
- Preserve the new pass13 matched channel result as evidence that the first `EMG1-EMG2` rerun on the strongest current `A1-only` scaffold did **not** beat `C4-P4`; it missed both bruxism subjects under LOSO and should be treated as a first-class negative result [[bruxism-cap-emg-vs-c4-a1-only-matched14-2026-05-04]].
- Preserve the new pass14 EMG-family result as evidence that `EMG1-EMG2` prefers exclusive `A3-only` over exclusive `A1-only` on window-level LOSO, but still fails the honest subject-level test under both families [[bruxism-cap-emg-a1-vs-a3-matched14-2026-05-04]].
- Preserve the new pass15 threshold audit as evidence that the stronger `EMG1-EMG2 A3-only` run fails because subject score ordering is wrong, not because the default `0.5` threshold is too strict [[bruxism-cap-emg-a3-threshold-audit-2026-05-04]].
- Preserve the new pass16 feature-validity audit as evidence that the high-score controls repeatedly benefit from `ratio_alpha_delta`, `min`, and `sample_entropy`, while `brux1` is dragged by extreme absolute-power / mean terms [[bruxism-cap-emg-feature-validity-audit-2026-05-04]].
- Preserve the new pass17 time-domain ablation as evidence that simply deleting the spectral / ratio family is not enough: honest subject-level sensitivity stays `0.000`, the best LOSO window-level result regresses slightly, and `n3` becomes even harder to separate [[bruxism-cap-emg-time-only-ablation-2026-05-04]].
- Preserve the new pass18 replacement-family rerun as evidence that add-only EMG-oriented envelope / burst features are also not enough by themselves: `brux1` and `brux2` recover slightly relative to pass17 and `n3` falls, but `n5` still outranks `brux1`, subject-level sensitivity stays `0.000`, and the stronger pass14 LOSO window-level result is still not beaten [[bruxism-cap-emg-envelope-replacement-2026-05-04]].
- Preserve the new pass19 selection-aware rerun as evidence that the pass18 envelope / burst family works better when the older spectral / ratio family is excluded at train time, but still does not cross the honest subject-level bar: `n5` improves relative to pass18 and the best LOSO balanced accuracy recovers to `0.629`, yet `n3` and `n5` still outrank `brux1` and subject-level sensitivity remains `0.000` [[bruxism-cap-emg-envelope-selection-2026-05-04]].
- Preserve the new pass20 mean-ablation rerun as evidence that the next bottleneck is **not** solved by one more naive deletion: once the older spectral / ratio family is already excluded, removing raw `mean` leaves `n3` and `n5` essentially unchanged but makes `brux1` collapse from `0.151` to `0.018`, so the stronger pass19 recipe should remain the EMG-first working point [[bruxism-cap-emg-mean-ablation-2026-05-04]].
- Preserve the new pass21 retained-family audit as evidence that the pass19 envelope / burst working point is doing something real but still not cleanly bruxism-aligned: `sample_entropy` and `burst_fraction` still help the highest-score controls, while `brux1`'s larger raw `rectified_mean`, `envelope_mean`, and `p95_abs` remain net-negative under the learned coefficients, so the next EMG-only move should be normalization-aware rather than another blind deletion [[bruxism-cap-emg-envelope-family-audit-2026-05-04]].
- Preserve the new pass22 normalization-aware rerun as evidence that simple per-window `median_mad` extraction is **not** that fix: on the same matched `EMG1-EMG2 A3-only` scaffold with the same pass19 train-time exclusions, best LOSO balanced accuracy regresses from `0.629` to `0.571`, subject-level sensitivity stays `0.000`, and both bruxism subjects rank even lower than under pass19 [[bruxism-cap-emg-median-mad-normalization-2026-05-04]].
- Preserve the new pass23 shared subject-score comparison as evidence that the current EMG gap is now sharper than “EMG is just worse”: pass19 does improve `brux1` relative to the honest pass12 `C4-P4 A1-only` anchor, but it loses decisively overall because `brux2` collapses (`0.795` -> `0.088`) and `n3` becomes the highest-score control, swinging the best-bruxism-minus-highest-control margin from `+0.362` to `-0.129` [[bruxism-cap-emg-pass19-vs-c4-pass12-subject-scores-2026-05-05]].
- Preserve the new pass24 focused gap audit as evidence that the current EMG failure is now localized more tightly than a generic channel loss: the decisive reversal is `brux2` falling below `n3`, `zero_crossing_rate` is the largest surviving control-favoring feature gap on the fixed pass19 scaffold, and the matched-14 extraction is still count-matched but not time-position-matched [[bruxism-cap-emg-brux2-vs-n3-gap-2026-05-05]].
- Preserve the new pass25 shared-time-position rerun as evidence that the pass24 timing concern was real but incomplete: forcing a shared absolute time interval improves both bruxism subjects (`brux1` `0.151` -> `0.282`, `brux2` `0.088` -> `0.215`) yet still leaves all three controls above both bruxism subjects and does not improve honest subject-level sensitivity beyond `0.000` [[bruxism-cap-emg-a3-time-position-matched-rerun-2026-05-05]].
- Preserve the new pass26 matched strict-scaffold comparison as evidence that the pass25 negative result is **not** a reason to abandon the EMG-first framing: rebuilding the exact same shared-time-position `A3-only` scaffold on `C4-P4` leaves subject-level sensitivity at `0.000`, worsens subject-level balanced accuracy from `0.500` to `0.333`, and turns `n3` into a hard false positive, so the stricter scaffold itself now looks like the bigger problem rather than `EMG1-EMG2` alone [[bruxism-cap-c4-vs-emg-timepos-a3-2026-05-05]].
- Preserve the new pass27 strict-`A1-only` feasibility audit as evidence that the pass25/pass26-style global shared-time-position rule is itself too brittle for the current `EMG1-EMG2 A1-only` subset: the uncapped pool exists (`233` rows total), but the common interval leaves only `2` windows per subject, so the next timing-aware rerun should soften the selector rather than pretending a `10`-row benchmark is informative [[bruxism-cap-emg-a1-time-position-feasibility-2026-05-05]].
- Research update on 2026-05-04: the latest translational literature is pushing harder toward portable jaw-muscle EMG, multi-night monitoring, and wearable sensing, but I did not find a clearly better open benchmark dataset than CAP. All 6 current local CAP EDFs do contain `EMG1-EMG2`, so the next project shape should likely be an EMG-first pivot inside the same reproducible CAP scaffold rather than a full dataset switch. [[bruxism-better-project-and-data-options-2026-05-04]].
- Preserve the new pass28 percentile-band rerun as evidence that softer timing control was the right extraction-validity patch for `EMG1-EMG2 A1-only`: the selector recovers a reproducible `10`-windows-per-subject scaffold, but the honest LOSO subject surface still fully collapses with both bruxism subjects below all controls [[bruxism-cap-emg-a1-percentile-band-rerun-2026-05-05]].
- Preserve the new pass29 matched percentile-band comparison as evidence that channel choice still matters on that repaired scaffold: rebuilding the exact same `A1-only` percentile-band subset on `C4-P4` raises honest subject-level balanced accuracy from `0.500` to `0.750` and recovers `brux2` without false positives, yet still only ties the older best honest baseline because `brux1` remains just below `n3` [[bruxism-cap-c4-a1-percentile-band-vs-emg-2026-05-05]].
- Preserve the new pass30 cross-channel gap audit as evidence that the repaired percentile-band `A1-only` scaffold is genuinely timing-matched across `EMG1-EMG2` and `C4-P4`, so the remaining honest bottleneck is now narrower than a channel-only story: `brux1` still trails `n3` under both channels, while the main matched gap is `brux2` recovering only on `C4-P4` [[bruxism-cap-a1-percentile-band-channel-gap-audit-2026-05-05]].
- Preserve the new pass31 recurrence audit as evidence that the suspected narrow `n3`-favoring trio (`sample_entropy`, `burst_fraction`, `envelope_cv`) is real but not sufficient on the repaired percentile-band scaffold: `burst_fraction` recurs, but the strongest EMG control-favoring gap is still broader and led by `mean`, `max`, `ptp`, and `zero_crossing_rate` [[bruxism-cap-a1-percentile-band-n3-family-recurrence-2026-05-05]].
- Preserve the new pass32 broader morphology ablation as evidence that the obvious wider deletion is not the fix either: on the same repaired `A1-only` scaffold, removing `mean`, `max`, `ptp`, `zero_crossing_rate`, `sample_entropy`, `burst_fraction`, and `envelope_cv` still leaves `EMG1-EMG2` below the honest baseline and also destroys the useful `brux2` recovery on `C4-P4` [[bruxism-cap-emg-a1-broad-morphology-ablation-2026-05-05]].
- Preserve the new pass33 smaller raw-location ablation as evidence that a narrower deletion is not the fix either: on the same repaired `A1-only` scaffold, removing only `mean`, `min`, and `max` leaves `C4-P4` essentially unchanged but makes `EMG1-EMG2` markedly worse by collapsing `brux1` (`0.270` -> `0.030`) while `n3` stays high (`0.530` -> `0.527`) [[bruxism-cap-emg-a1-raw-location-ablation-2026-05-05]].
- Preserve the new pass34 record-relative audit as evidence that representation changes can help one part of the repaired scaffold without solving the whole benchmark: within-record robust feature scaling removes the `n3` false positive and flips `brux2 - n3` from `-0.494` to `+0.041`, but `brux1` still falls from `0.270` to `0.180`, so subject-level sensitivity remains `0.000` [[bruxism-cap-emg-a1-record-relative-audit-2026-05-05]].
- Preserve the new pass35 matched `C4-P4` comparator as evidence that the same record-relative transform is not a channel-agnostic fix: it lifts `brux1` above threshold but collapses the earlier `brux2` recovery (`0.959` -> `0.262`) and turns `n3` into a new false positive, so the pass34 gain now looks more EMG-specific than universal [[bruxism-cap-c4-record-relative-comparator-2026-05-05]].
- Preserve the new pass35 EMG shape-feature expansion as evidence that a compact waveform-shape family can also improve the repaired scaffold without subject-subset drift: the rebuilt pass27/pass28 counts and selected rows match exactly, `n3 - brux1` shrinks from `+0.260` to `+0.009`, and `brux2 - n3` flips to `+0.174`, but both bruxism subjects still stay below threshold so subject-level sensitivity remains `0.000` [[bruxism-cap-emg-a1-shape-feature-expansion-2026-05-05]].
- Preserve the new pass36 composition audit as evidence that the two best current EMG representation clues really do compose on the repaired scaffold: stacking pass35 shape on top of the pass34 record-relative table lifts subject-level balanced accuracy from `0.500` to `0.750`, recovers subject-level sensitivity to `0.500`, and pushes `brux2` clearly above threshold, but `brux1` still regresses and remains the remaining bottleneck [[bruxism-cap-record-relative-shape-composition-audit-2026-05-05]].
- Preserve the new pass36 `brux1` localization audit as evidence that the remaining miss is now narrower than the earlier `n3` inversion story: on the composed scaffold `brux1` actually edges above `n3` (`0.112` vs `0.068`), but still trails `n5` and `n11` badly while `brux2` is strongly rescued, so the unresolved issue is a `brux1`-specific under-scoring surface inside the record-relative amplitude / dispersion family rather than a generic EMG failure [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- Preserve the new patched-EMG versus matched-`C4-P4` synthesis as scaffold evidence rather than as the final EMG anchor decision: `C4-P4` still wins the narrow honest subject-level criterion and both channels remain bottlenecked by the repaired scaffold, but the later honest-anchor verdict shows that the useful forward question is whether the pass34 and pass35 EMG gains compose honestly, not whether `pass35` alone should be promoted as the settled EMG anchor [[bruxism-cap-patched-emg-vs-matched-c4-anchor-comparison-2026-05-05]] [[bruxism-cap-pass35-shape-vs-honest-anchors-verdict-2026-05-05]].
- Research update on 2026-05-05: a periodic translational framing check still supports the current repo posture. The newer literature is more strongly wearable, multi-night, and intervention-aware, but it still does not provide a clearly better open benchmark than CAP, so `EMG1-EMG2` should remain the primary benchmark channel while portable/home/intervention EMG stays a future branch rather than an immediate pivot [[bruxism-cap-translational-framing-check-2026-05-05]].
- Future-branch note on 2026-05-05: privacy-preserving wearable EMG design is now an explicitly captured downstream branch for this project and should be treated as one of the main ways this work stands apart from generic bruxism-detection projects. It belongs in the wiki first as architecture/threat-model/prototype planning, then should move onto the board promptly once the current benchmark reaches a good-enough state [[bruxism-cap-privacy-pets-roadmap-2026-05-05]].
- Second future-branch note on 2026-05-05: LLM/fine-tuning/RL work is also now an explicitly captured downstream branch, but it should be framed as privacy-aware adaptive wearable intelligence rather than generic AI garnish. It belongs in the wiki first as roadmap/design/prototype planning, then should move onto the board through one bounded memo or prototype task at a time once the benchmark stabilizes [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- Privacy-branch activation note on 2026-05-05: after pass36 answered the campaign's last pre-privacy composition question, the repo activated exactly one bounded privacy branch in memo form rather than as a benchmark pivot. The activation reason is that the benchmark is now stable enough to preserve repo-specific privacy constraints while still unfinished: CAP remains the public scaffold, `EMG1-EMG2` remains the primary benchmark channel, `pass29 C4-P4` remains the honest comparison anchor, and `brux1` remains the active bottleneck [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- The privacy memo concluded that once the project moves beyond public CAP toward private wearable jaw EMG, raw waveform, dense timing structure, full per-window feature rows, stable identifiers, and personalized calibration should stay local by default; the first off-device layer should instead be minimized nightly/weekly/cohort summaries rather than CAP-style dense artifacts [[bruxism-cap-privacy-threat-model-activation-2026-05-05]].
- Exact next privacy task gate on 2026-05-05: do not broaden into federated/product/privacy implementation yet. Keep the main board on the current `brux1` benchmark bottleneck, and only promote the next privacy task — `privacy: design a minimized off-device export schema for future wearable bruxism-cap experiments` — once the current benchmark reaches a handoff or temporary stabilization point after that bounded `brux1` localization loop [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-privacy-pets-roadmap-2026-05-05]].
- After the new pass33 smaller raw-location ablation, the next bounded target should stay leakage-aware and EMG-first but shift more clearly from deletion toward representation: keep the repaired `A1-only` scaffold fixed and prefer a record-relative or within-record morphology audit before another feature-removal pass.
- After the new pass34 record-relative audit, the next bounded target should stay on the same repaired scaffold but move to the backup shape-family test: preserve the mixed normalization result, keep the selector/model family fixed again, and add one compact shape-only family (`skewness`, `kurtosis`, `hjorth_mobility`, `hjorth_complexity`) rather than stacking more normalization variants.
- After the new pass35 matched `C4-P4` comparator, the next bounded target should remain EMG-first: preserve the comparator as evidence that record-relative normalization is not a universal cross-channel fix, and test the compact shape-family backup on the repaired EMG scaffold rather than promoting the transform itself to the default matched benchmark.
- After the new pass35 honest-anchor verdict, the next bounded target should preserve both mixed representation results but stop treating pass34-versus-pass35 as the whole question: add the same four compact shape features on top of the pass34 record-relative scaffold, then run a strict pass34 versus `record-relative + shape` LOSO comparison on the same repaired five-subject split before any broader feature, normalization, or channel change.
- Campaign-level synthesis on 2026-05-05 preserved the stronger board read: do not pause or pivot yet. Keep CAP plus `EMG1-EMG2` as the bounded benchmark frame, keep `pass29 C4-P4` as the honest comparison anchor, and continue the autoresearch loop narrowly through one `record-relative + shape` composition audit before promoting privacy/PET, LLM/RL, or broader benchmark-reframing branches [[bruxism-cap-campaign-handoff-2026-05-05]].
- After the new pass36 composition audit, the next bounded target should stay on the same repaired five-subject scaffold and localize why `brux1` still fails while `brux2` now clears threshold, rather than pivoting immediately to another channel or stacking a broader feature family.
- After the new pass36 `brux1` localization audit, the safest next increment should stay on the exact current pass36 table: compare `brux1` directly against `n5` and `n11` fold-by-fold, verify whether the miss is uniformly low or sparse-window low, and test whether the remaining suppression is concentrated in the record-relative amplitude / dispersion block rather than the Hjorth shape block before any new rerun or channel pivot [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- A deeper same-day fold audit then resolved that exact question on the fixed table: `brux1` is not uniformly low, but instead has a catastrophic early three-window collapse plus a weak residual floor, and the dominant suppression against both `n5` and `n11` still lives overwhelmingly in the record-relative amplitude / dispersion block rather than in the added shape family.
- After that deeper fold audit, the next bounded benchmark increment should narrow again without broadening the scaffold: keep pass36 fixed, isolate the early `brux1` trio against the same subject's mid/late windows, and run one amplitude-stabilization audit on the record-relative amplitude / dispersion family before any new rerun, channel pivot, or future-branch activation [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- Preservation update after the bounded post-pass36 audit: the project understanding changed narrowly but materially. The campaign's earlier composition question is now closed, and the active benchmark bottleneck is explicitly `brux1` under-scoring versus `n5` and `n11` on the fixed pass36 composed scaffold, not a generic `n3` inversion or a generic EMG collapse [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- Exact next benchmark task after that deeper fold audit: stay on the exact pass36 table, compare the catastrophic early `brux1` trio against the same subject's later windows, and test one very small cap or robust clipping rule on the record-relative amplitude / dispersion block while keeping selected rows, model family, train-time exclusions, and the compact shape block fixed. Do not blur that task with another rerun, a channel pivot, privacy implementation, or synthetic-data / LLM-RL work [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- The new pass37 tiny amplitude-stabilization audit preserved a useful but limited negative result on that exact scaffold: softly upper-clipping only `mean`, `rectified_std`, and `envelope_cv` at `2.5` slightly lifts `brux1` (`0.112` -> `0.118`) and lowers `n5` / `n11` without reopening the old `n3` reversal, but the catastrophic early `brux1` trio still stays essentially near zero and the honest subject-level verdict remains unchanged [[bruxism-cap-pass37-tiny-amplitude-stabilization-audit-2026-05-05]].
- After pass37, the safest next bounded benchmark increment should stay on the same repaired five-subject scaffold but move the stabilization test one stage earlier in the same feature family: keep the pass35 shape merge fixed and audit a bounded robust-scale-floor variant inside the pass34-style record-relative transform for `mean`, `rectified_std`, and `envelope_cv`, rather than broadening into another channel or feature-family change [[bruxism-cap-pass37-tiny-amplitude-stabilization-audit-2026-05-05]].
- Preservation update after the bounded pass37 follow-up: the benchmark story is now slightly sharper. A tiny post-table clip can soften the localized `brux1` collapse while preserving the repaired control surface, but it is not enough to rescue the benchmark, so the remaining uncertainty lives in the record-relative amplitude construction itself more than in the later shape merge [[bruxism-cap-pass37-tiny-amplitude-stabilization-audit-2026-05-05]] [[bruxism-cap-record-relative-shape-composition-audit-2026-05-05]].
- The new pass38 earlier-stage scale-floor audit preserved another useful narrow negative result on the same scaffold: recomputing only `envelope_cv` with a tail-aware floor `max(MAD, 0.5 * q90(|x - median|))` inside the pass34-style transform nudges `brux1` from `0.112` to `0.115` and keeps `n3` safely below it, but it does not beat pass37 and still leaves `n5` / `n11` well above `brux1`, so `envelope_cv` scale alone is not the missing fix [[bruxism-cap-pass38-envelope-cv-scale-floor-audit-2026-05-05]].
- After pass38, the safest next bounded benchmark increment should remain on the same repaired five-subject scaffold and widen only one notch inside the same record-relative amplitude family: keep the pass35 shape merge fixed again and apply the same style of robust-scale floor to `envelope_cv` plus exactly one stronger recurring offender such as `rectified_std` or `mean`, rather than broadening into a channel pivot, feature-family expansion, or future-branch activation [[bruxism-cap-pass38-envelope-cv-scale-floor-audit-2026-05-05]].
- Preservation follow-through after the bounded pass37/pass38 subloop: the early `brux1` collapse did soften directionally, but not materially. Pass37 improved the early trio and pass38 confirmed that the effect survives an earlier-stage test, yet both remain far from a threshold-relevant rescue and leave the catastrophic windows essentially near zero.
- The deeper diagnosis therefore holds: the dominant residual suppression still lives in the record-relative amplitude / dispersion block rather than in the added shape family, with `n5` and `n11` still acting as the active control-side bottleneck on the repaired scaffold [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]] [[bruxism-cap-pass37-tiny-amplitude-stabilization-audit-2026-05-05]] [[bruxism-cap-pass38-envelope-cv-scale-floor-audit-2026-05-05]].
- Exact next benchmark task after the robust-scale-floor audit: keep the same repaired five-subject scaffold, selected rows, train-time exclusions, pass35 shape merge, and evaluation contract fixed, then run one equally bounded earlier-stage multi-feature audit that applies the pass38-style robust-scale floor to `envelope_cv` together with exactly one stronger recurring offender from the same block (`rectified_std` or `mean`). Do not blur that task with privacy work, synthetic-data work, an LLM/RL prototype, a channel pivot, or a broad feature expansion.
- Future-branch gate status after the bounded robust-scale-floor audit: unchanged and still closed. The next privacy task remains `privacy: design a minimized off-device export schema for future wearable bruxism-cap experiments`, and the first bounded LLM/RL memo tasks remain wiki-first only; neither should move to the board until the benchmark reaches a real handoff or temporary stabilization point after this still-narrow record-relative follow-up [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- The new pass39 paired earlier-stage floor audit preserved the strongest remaining two-feature follow-through as another useful negative result: `mean` was the right next offender to test because it dominates the `n5` / `n11` gaps versus `brux1`, but applying the same bounded floor to `envelope_cv + mean` still lifts `brux1` only to `0.114`, does not beat pass37 (`0.118`), and leaves the honest subject-level verdict unchanged at `0.750` balanced accuracy, `0.500` sensitivity, and `1.000` specificity [[bruxism-cap-pass39-envelope-cv-mean-scale-floor-audit-2026-05-05]].
- Pass39 still matters diagnostically because it softens the catastrophic early `brux1` trio more than pass38 did (`-194.660` -> `-162.058` on early amp/disp mean) without reopening the repaired `n3` surface, yet `n5` and `n11` remain essentially unchanged. That means `mean` really is part of the early collapse, but `envelope_cv + mean` scale alone is still not the threshold-relevant missing fix [[bruxism-cap-pass39-envelope-cv-mean-scale-floor-audit-2026-05-05]] [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- Exact next benchmark task after pass39: keep the same repaired five-subject scaffold, selected rows, train-time exclusions, pass35 shape merge, and evaluation contract fixed once more, and run one last equally bounded earlier-stage audit that applies the same floor style to `envelope_cv + rectified_std`. Do not blur that task with privacy work, synthetic-data work, an LLM/RL prototype, a channel pivot, or a broad feature expansion [[bruxism-cap-pass39-envelope-cv-mean-scale-floor-audit-2026-05-05]].
- Future-branch gate status after pass39: still unchanged and still closed. The next privacy task remains `privacy: design a minimized off-device export schema for future wearable bruxism-cap experiments`, and the first bounded LLM/RL memo tasks remain wiki-first only; neither should move to the board until the benchmark reaches a real handoff or temporary stabilization point after this final same-family follow-through [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- The new pass40 final same-family follow-through preserved the stronger pass39 diagnosis while closing the exact `envelope_cv` companion-floor subloop: swapping the bounded earlier-stage floor companion from `mean` to `rectified_std` leaves `brux1` essentially flat (`0.112` -> `0.112`), keeps the honest subject-level verdict unchanged, and therefore does not beat pass37 or pass39 on the target subject [[bruxism-cap-pass40-envelope-cv-rectified-std-scale-floor-audit-2026-05-05]].
- Pass40 still adds one useful localized read: the early catastrophic `brux1` trio softens a little relative to pass36 and pass38, but less than under pass39, and the `brux1 - n3` margin shrinks to only `+0.006` while `n5` and `n11` remain clearly above `brux1`. So `rectified_std` participates in the collapse, but this same-family earlier-stage floor idea now looks exhausted as a rescue path on the repaired scaffold [[bruxism-cap-pass40-envelope-cv-rectified-std-scale-floor-audit-2026-05-05]] [[bruxism-cap-pass39-envelope-cv-mean-scale-floor-audit-2026-05-05]].
- Exact next benchmark task after pass40: do not run another companion swap inside this exact `envelope_cv` floor family. Preserve pass37 as the safest bounded stabilization surface inside the current scaffold, then choose one new bounded representation question outside this now-closed floor subloop before considering any broader branch activation [[bruxism-cap-pass40-envelope-cv-rectified-std-scale-floor-audit-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- Future-branch gate status after pass40: unchanged and still closed. The next privacy task remains `privacy: design a minimized off-device export schema for future wearable bruxism-cap experiments`, and the first bounded LLM/RL memo tasks remain wiki-first only; neither should move to the board until a later benchmark increment outside this floor subloop produces a real handoff point [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- Fresh literature update after pass40: the strongest bounded next move is now one compact event-conditioned jaw-EMG block on top of the unchanged pass36 table rather than another coarse 30 s amplitude-summary tweak. The source-verified rationale is that classic and portable jaw-EMG studies define sleep bruxism through subject-relative burst and episode structure, RMMA work distinguishes single events versus clusters, and newer critiques warn against collapsing that structure into one episode index or overclaiming from EMG-only scoring [[bruxism-cap-pass41-event-conditioned-feature-block-2026-05-05]].
- Exact next benchmark task after the pass41 literature memo: keep the CAP scaffold, `EMG1-EMG2` primary channel, repaired five-subject rows, train-time exclusions, and LOSO framing fixed, then compare `pass36` versus `pass36 + 7` appended event-conditioned features (`evt_burst_count_30s`, `evt_episode_count_30s`, `evt_bursts_per_episode_mean`, `evt_active_fraction`, `evt_burst_duration_median_s`, `evt_interburst_gap_median_s`, `evt_phasic_like_episode_fraction`) before any broader branch activation [[bruxism-cap-pass41-event-conditioned-feature-block-2026-05-05]].
- The new pass41 bounded audit answered that exact question with a mixed but useful result: the appended event-conditioned block does lift `brux1` slightly and softens the catastrophic early trio, but it also reopens the control surface by pushing `n11` above threshold, so the honest subject-level verdict regresses from `0.750` to `0.583` balanced accuracy and specificity falls from `1.000` to `0.667` [[bruxism-cap-pass41-event-conditioned-feature-block-audit-2026-05-05]].
- The deeper pass41 read is narrower than “event features failed.” Against `n5`, the event block is net-helpful to `brux1`, but the raw seven-feature bundle is not selective enough on this tiny scaffold and still leaves the old record-relative amplitude / dispersion block as the dominant gap driver. So the safest next move is one same-table event-block ablation or light rescaling pass, not another broad feature expansion or a branch pivot [[bruxism-cap-pass41-event-conditioned-feature-block-audit-2026-05-05]] [[bruxism-cap-brux1-localization-after-pass36-2026-05-05]].
- Separate benchmarking-methodology update on `2026-05-05`: a fresh tiny-subject validation scan first narrowed the reporting contract more than the feature contract, and the smallest code patch is now landed in the lightweight pipeline. Keep grouped `LOSO` plus subject-level aggregation as the primary honest endpoint, keep random-window CV only as a leakage-reference surface, and preserve the new subject-level uncertainty/calibration additions in `train_baseline.py`: raw denominator counts, exact and Wilson `95%` intervals for subject-level sensitivity / specificity, a subject-level Brier score, and explicit per-subject mean positive probabilities alongside the older `mean_score` field [[bruxism-cap-tiny-subject-benchmarking-upgrade-2026-05-05]].
- Combined preservation verdict after the pass41 audit plus the benchmark-reporting patch: the repo learned more than it improved. `pass41` is diagnostically stronger than another same-family floor swap because it slightly lifts `brux1` and localizes the reopened failure to `n11`, but it is still not a material honest benchmark gain over `pass36` or `pass40`, and it still does not beat the stronger matched `pass29 C4-P4` comparator context [[bruxism-cap-pass41-vs-honest-anchors-benchmark-upgrade-verdict-2026-05-05]].
- The new reporting outputs do improve interpretability even though they do not change the benchmark verdict: for new artifacts like `pass41`, the repo can now preserve denominator counts, exact/Wilson `95%` intervals, subject-level Brier score, and explicit `mean_positive_probability` values, which makes tiny-sample negative results easier to trust and compare. The remaining caveat is that older anchors such as `pass29 C4-P4` still predate that patch, so interpretability is improved first on the new EMG artifacts rather than backfilled uniformly across the full history [[bruxism-cap-pass41-vs-honest-anchors-benchmark-upgrade-verdict-2026-05-05]] [[bruxism-cap-tiny-subject-benchmarking-upgrade-2026-05-05]].
- Exact next benchmark task after this combined review: stay on the same pass41 table and run exactly one bounded event-block ablation that keeps only the least control-damaging subset, with the primary goal of preserving the small `brux1` lift while pushing `n11` back below threshold. Do not use this mixed result to activate privacy or LLM/RL follow-up tasks yet; those future branches remain gated because the evidence sharpened diagnosis but did not create a real benchmark handoff point [[bruxism-cap-pass41-vs-honest-anchors-benchmark-upgrade-verdict-2026-05-05]] [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- The new pass42 same-table ablation is the cleanest local event follow-through so far: keeping only `evt_active_fraction`, `evt_burst_duration_median_s`, and `evt_interburst_gap_median_s` on the reused pass41 table restores the honest subject-level surface to `0.750` balanced accuracy and `1.000` specificity, pushes `n11` back below threshold (`0.486`), and lifts `brux1` further to `0.136`, which is better than pass41 and slightly better than pass36/pass40 on the target subject even though `brux1` still remains below threshold and still trails `n3` [[bruxism-cap-pass42-same-table-event-subset-ablation-2026-05-05]].
- That means the branch should stay event-subset-first rather than reopening the broader 7-feature bundle or returning to the closed floor family. The pass42 result says the event idea survives once narrowed, but it still does not justify future-branch activation or model-family change because the honest subject-level sensitivity is still only `0.500` and `brux1` still does not clear threshold [[bruxism-cap-pass42-same-table-event-subset-ablation-2026-05-05]] [[bruxism-cap-pass41-event-conditioned-feature-block-audit-2026-05-05]].
- Exact next bounded benchmark task after pass42 was the transfer check: keep the verified 3-feature event subset fixed and compare the same subset on `EMG1-EMG2` `A1-only` versus the closest existing `A3-only` surface before changing model family, channel family, privacy scope, or LLM/RL scope. That question was narrower than another same-table ablation because the local subset choice was already good enough to test transfer rather than re-search selection [[bruxism-cap-pass42-same-table-event-subset-ablation-2026-05-05]] [[bruxism-cap-emg-a1-vs-a3-matched14-2026-05-04]].
- The new pass43 transfer audit answered that question in a mixed but still useful way: `brux1` itself roughly holds on the old matched14 `A3-only` surface (`0.176`), and `n3` / `n5` both improve versus the old pass14 `A3-only` baseline, but the honest transfer still collapses overall because subject-level sensitivity stays `0.000`, balanced accuracy stays `0.500`, and `brux2` remains far below the repaired pass42 `A1-only` anchor [[bruxism-cap-pass43-matched-a1-vs-a3-transfer-audit-2026-05-05]].
- That means the current branch read is now tighter: the verified 3-feature event subset is not pure `A1-only` noise, but the repaired pass42 gain does not yet transfer honestly across the full existing `A3-only` surface. The next uncertainty is scaffold mismatch more than subset identity, because the current comparison is still `50` repaired `A1-only` rows versus `70` older matched14 `A3-only` rows on the same channel family [[bruxism-cap-pass43-matched-a1-vs-a3-transfer-audit-2026-05-05]].
- Board-level preservation update after the pass43 verdict synthesis: the most honest durable label is now `scaffold-bound`, not `A1-specific` and not `A3-transferable`. The literature lane changed the interpretation by making the 3 retained event features look more like burst-organization descriptors than pure family-locked amplitude quirks, while still warning that CAP family filters also change row availability and timing surfaces in this repo [[bruxism-cap-pass43-event-subset-family-verdict-2026-05-05]].
- The negative findings remain first-class: honest `A3-only` transfer still fails at `0.500` balanced accuracy and `0.000` sensitivity, `brux2` collapses from the repaired pass42 anchor (`0.825` -> `0.130`), and `n11` worsens sharply on `A3` even though it stays below threshold. So pass42 should stay the active EMG working surface only inside the repaired `A1-only` branch, not as a family-general winner [[bruxism-cap-pass43-event-subset-family-verdict-2026-05-05]] [[bruxism-cap-c4-a1-percentile-band-vs-emg-2026-05-05]].
- Exact next bounded benchmark task after the preserved pass43 verdict: keep the same 3-feature event subset fixed and rebuild only the `A3-only` comparison table on the repaired percentile-band / time-aware `EMG1-EMG2` scaffold before any broader feature, model, privacy, or LLM/RL change. Privacy and LLM/RL branches remain gated because the subset gain is directional but still scaffold-sensitive rather than handoff-grade [[bruxism-cap-pass43-event-subset-family-verdict-2026-05-05]] [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- Future-branch gate status after pass43: still unchanged and still closed. The privacy export-schema task and the first bounded LLM/RL memo tasks remain wiki-first and gated until a later benchmark increment converts this directional but still scaffold-sensitive subset gain into a real threshold-relevant handoff point [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- Operational board policy after the pass43 dispatch confusion: `ai-lab` is now the default specifier for `TRIAGE` cards on `bruxism-cap`; `TRIAGE` is explicitly a parked intake/spec lane rather than an auto-dispatched queue; and the currently picked branch-spec card is `t_98c3f05e`, whose job is to narrow the next active branch to a repaired `A3-only` scaffold rebuild rather than a broad new feature search [[bruxism-cap-kanban-policy-and-triage-2026-05-05]].
- The new pass44 repaired `A3-only` rebuild resolves the main ambiguity left by pass43: once the repo rebuilds the `EMG1-EMG2` `A3-only` table on the repaired percentile-band / time-aware scaffold instead of reusing the old matched14 surface, the honest subject-level headline returns to `0.750` balanced accuracy, `0.500` sensitivity, and `1.000` specificity — exactly matching the pass42 repaired `A1-only` anchor [[bruxism-cap-pass44-repaired-a3-event-subset-rebuild-2026-05-05]].
- A fresh post-pass44 methodology review tightened what the benchmark still does **not** say: exact binomial subject-level CIs remain necessary, but they are now identical across repaired `pass42` and repaired `pass44`, so the sharper next honesty upgrade is a paired repaired-surface audit that makes visible which bruxism subject is rescued under `A1-only` versus `A3-only` while the same `1/2` and `3/3` headline counts are preserved [[bruxism-cap-pass42-pass44-cross-family-methodology-review-2026-05-05]].
- That means the current repaired cross-family read should be "count-matched but subject-unstable," not simply "A1 and A3 are equivalent": the repaired `A3-only` run rescues `brux1` while the repaired `A1-only` run rescues `brux2`, and the paired subject-surface margin still shrinks from `+0.339` on repaired `A1-only` to `+0.138` on repaired `A3-only` [[bruxism-cap-pass42-pass44-cross-family-methodology-review-2026-05-05]].
- That means the pass43 negative transfer verdict was primarily a scaffold-mismatch artifact rather than evidence that the retained event subset was `A1`-locked. The repaired `A3-only` table now matches the repaired `A1-only` scaffold at `50` final rows total (`10` per subject) instead of comparing `50` repaired rows against the older `70`-row matched14 `A3-only` surface [[bruxism-cap-pass44-repaired-a3-event-subset-rebuild-2026-05-05]] [[bruxism-cap-pass43-matched-a1-vs-a3-transfer-audit-2026-05-05]].
- A same-day follow-up audit then localized the repaired cross-family asymmetry more precisely: the benchmark is no longer bottlenecked by whether the fixed 3-feature event subset transfers across `A1-only` and `A3-only`, but by which bruxism subject the repaired `A3-only` surface rescues. On pass44, `brux1` rises sharply to `0.532` while `brux2` stays collapsed at `0.123`; the strongest evidence points to the retained record-relative amplitude / dispersion block as the main carrier of that split, with the compact shape block as a narrower next ablation target rather than another scaffold rewrite [[bruxism-cap-pass44-cross-family-asymmetry-audit-2026-05-05]].
- The benchmark is still only partially rescued, not solved: pass44 confirms cross-family recovery up to the pass42 ceiling, but not beyond it. `brux1` is recovered while `brux2` still stays below threshold, so subject-level sensitivity remains `0.500`, and the future privacy / LLM-RL branches should remain gated until a later bounded step converts this repaired-scaffold recovery into an actual threshold-relevant gain [[bruxism-cap-pass44-repaired-a3-event-subset-rebuild-2026-05-05]] [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- After pass44, the sharper next target is now to keep the repaired cross-family scaffold fixed and test only one similarly bounded change aimed at improving the collapsed `brux2` verdict without sacrificing the new `brux1` rescue or reopening the control surface, rather than revisiting whether the retained 3-feature event subset transfers beyond `A1-only`.
- A fresh three-lane synthesis then fixed the durable read more tightly: the repaired cross-family branch is now `count-matched but subject-unstable`, not transfer-failed. The fixed 3-feature event subset survives as the validated base block, but the active repaired `A3-only` miss is `brux2` staying broadly low while `brux1` is rescued through the amplitude / dispersion surface [[bruxism-cap-pass45-next-step-synthesis-2026-05-05]].
- Exact next benchmark task after that synthesis: keep the repaired pass44 `A3-only` table fixed and run one same-table compact shape-family ablation dropping only `skewness`, `kurtosis`, `hjorth_mobility`, and `hjorth_complexity`, with the explicit goal of lifting `brux2` without sacrificing the pass44 `brux1` rescue or reopening controls [[bruxism-cap-pass45-next-step-synthesis-2026-05-05]] [[bruxism-cap-pass44-cross-family-asymmetry-audit-2026-05-05]].
- Exact next measurement/reporting upgrade after that synthesis: standardize a paired subject-surface audit whenever two runs share the same subjects, per-subject window counts, and evaluation contract, because repaired pass42 and pass44 now prove that exact subject-level CIs remain necessary but no longer differentiate the current branch on their own [[bruxism-cap-pass45-next-step-synthesis-2026-05-05]] [[bruxism-cap-pass42-pass44-cross-family-methodology-review-2026-05-05]].
- Backup branch status after that synthesis: the literature-backed `evt_bursts_per_episode_mean` add-back remains alive, but only as the next backup if the smaller same-table shape ablation fails cleanly; privacy and LLM/RL branches remain gated because the benchmark still stops at `0.500` subject-level sensitivity [[bruxism-cap-pass45-next-step-synthesis-2026-05-05]] [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- The new pass45 same-table no-shape ablation closes that exact test cleanly in the positive direction: keeping the repaired `A3-only` table frozen and dropping only `skewness`, `kurtosis`, `hjorth_mobility`, and `hjorth_complexity` preserves the honest `0.750 / 0.500 / 1.000` subject-level headline while lifting `brux1` from `0.532` to `0.641`, lifting `brux2` from `0.123` to `0.178`, lowering the highest control from `0.395` to `0.345`, and widening the repaired `A3-only` best-bruxism-minus-highest-control margin from `+0.138` to `+0.295` [[bruxism-cap-pass45-repaired-a3-shape-block-ablation-2026-05-05]].
- The paired pass45-versus-pass44 audit is now the concrete proof that the benchmark can improve meaningfully even when the tiny-N headline counts stay unchanged: no subject prediction flips occur relative to pass44, but the copied-through exact-CI / Brier block and paired score rows show that the repaired `A3-only` surface became materially cleaner rather than merely different [[bruxism-cap-pass45-repaired-a3-shape-block-ablation-2026-05-05]].
- Durable next-step read after pass45: preserve pass45 no-shape as the new repaired `A3-only` anchor and only then test the backup one-feature `evt_bursts_per_episode_mean` add-back on top of the frozen pass42/pass45 trio; privacy and LLM/RL branches remain gated because `brux2` is still below threshold and honest subject-level sensitivity is still `0.500` [[bruxism-cap-pass45-repaired-a3-shape-block-ablation-2026-05-05]] [[bruxism-cap-privacy-threat-model-activation-2026-05-05]] [[bruxism-cap-llm-rl-roadmap-2026-05-05]].
- A stricter post-pass45 benchmark-verdict memo then fixed the language that should be used downstream: the result is a real repaired-`A3-only` score-surface improvement, but the honest benchmark verdict should stay `ambiguous`, not `positive`, because the headline remains `0.750 / 0.500 / 1.000`, `brux1` alone clears the highest control while `brux2` stays below it, and repaired `A3-only` still trails repaired `A1-only` on the best-bruxism-minus-highest-control margin (`+0.295` versus `+0.339`) [[bruxism-cap-pass45-honest-benchmark-verdict-2026-05-05]].
- Exact next bounded benchmark task after that stricter verdict was the one-feature backup add-back: keep pass45 as the repaired `A3-only` anchor and restore only `evt_bursts_per_episode_mean` on top of the frozen pass42/pass45 trio while keeping the subject set, selector, threshold, channel family, and `logreg` LOSO contract fixed [[bruxism-cap-pass45-honest-benchmark-verdict-2026-05-05]] [[bruxism-cap-pass45-next-step-synthesis-2026-05-05]].
- Revised campaign-handoff update after the post-pass44 branch closed through pass45: the branch is no longer asking whether the repaired `A3-only` shape block should be removed, because that same-table question is now answered and preserved by `pass45`. The durable campaign read is to keep `pass45` as the repaired `A3-only` anchor, preserve the negative result that the honest benchmark still stays `ambiguous`, keep privacy and LLM/RL branch gates closed, and move next to exactly one one-feature add-back (`evt_bursts_per_episode_mean`) on the frozen pass42/pass45 scaffold [[bruxism-cap-campaign-handoff-2026-05-05]] [[bruxism-cap-pass45-honest-benchmark-verdict-2026-05-05]].
- The new pass46 one-feature add-back answered that exact backup question with a narrow mixed result: restoring only `evt_bursts_per_episode_mean` on the frozen repaired `A3-only` no-shape scaffold lifts `brux2` slightly (`0.178` -> `0.196`) and keeps all controls below threshold, but it also nudges `n5` upward (`0.337` -> `0.347`), leaves `brux1` essentially flat (`0.641` -> `0.639`), and slightly worsens the overall repaired `A3-only` best-bruxism-minus-highest-control margin (`+0.295` -> `+0.292`) [[bruxism-cap-pass46-one-feature-addback-2026-05-05]].
- That means `evt_bursts_per_episode_mean` remains directionally informative rather than useless noise, but the add-back is not yet a clean enough repaired-`A3-only` improvement to replace `pass45` as the durable anchor. The honest benchmark verdict therefore stays `ambiguous`, repaired `pass42` still owns the stronger `brux2` rescue, and `pass46` should be preserved as a side-variant negative-result memo rather than a promoted replacement [[bruxism-cap-pass46-one-feature-addback-2026-05-05]] [[bruxism-cap-pass46-honest-benchmark-verdict-2026-05-05]].

- After the new feature-validity audit, the next bounded target is narrower still: keep the matched `EMG1-EMG2 A3-only` scaffold fixed and test one small feature-family ablation before changing model family.
- After the time-domain ablation, the next bounded target is narrower again: keep the same matched `EMG1-EMG2 A3-only` scaffold, but move from deletion-only patches to one compact EMG-specific replacement family such as burst / envelope / amplitude-variability features.
- After the pass20 mean-ablation rerun, the next bounded target is narrower again: keep the stronger pass19 selection-aware EMG recipe fixed and audit the retained amplitude / envelope family directly (`rectified_*`, `envelope_*`, `p95_abs`, `sample_entropy`) because naive raw-`mean` removal does not lower the highest-score controls and instead makes `brux1` much worse.
- After the new pass21 retained-family audit, the next bounded EMG-only target is narrower again: keep the stronger pass19 selection-aware recipe fixed, preserve the retained envelope family, and test one normalization-aware extraction change before deleting more features or changing model family.
- After the new pass22 normalization-aware rerun, the next bounded target should shift back toward benchmark clarity: keep pass19 as the EMG-first working point and compare it directly against the honest pass12 `C4-P4 A1-only` anchor in one shared subject-score table before trying another extraction rewrite.
- After the new pass23 shared subject-score comparison, the next bounded target should narrow again: keep pass19 fixed and audit why `brux2` collapses under `EMG1-EMG2` while `n3` becomes the dominant control, instead of launching another broad feature or normalization rewrite.
- After the new pass24 focused gap audit, the next bounded target should narrow again without abandoning the EMG-first frame: keep pass19 fixed, treat the `brux2`-below-`n3` reversal as the main failure surface, and test a simple time-position-matched extraction variant before another broad feature rewrite.
- After the new pass25 shared-time-position rerun, the next bounded target should stay scaffold-matched but become channel-comparative again: rebuild the same shared-interval `10`-windows-per-subject subset on `C4-P4` so the repo can separate a channel effect from a stricter time-position-matched scaffold effect.
- After the new pass26 matched strict-scaffold comparison, the next bounded target stayed EMG-first and checked strict `EMG1-EMG2 A1-only` feasibility directly.
- After the new pass27 feasibility audit, the next bounded target should still stay EMG-first but soften the timing control: extend the current selector beyond one hard shared interval so `EMG1-EMG2 A1-only` can be audited with more than `2` windows per subject before another matched channel comparison.
- Revised campaign-handoff update after the post-pass44 branch closed through pass45: the branch is no longer asking whether the repaired `A3-only` shape block should be removed, because that same-table question is now answered and preserved by `pass45`. The durable campaign read is to keep `pass45` as the repaired `A3-only` anchor, preserve the negative result that the honest benchmark still stays `ambiguous`, keep privacy and LLM/RL branch gates closed, and move next to exactly one one-feature add-back (`evt_bursts_per_episode_mean`) on the frozen pass42/pass45 scaffold [[bruxism-cap-campaign-handoff-2026-05-05]] [[bruxism-cap-pass45-honest-benchmark-verdict-2026-05-05]].
- A fresh pass46 science review then source-verified why that exact one-feature add-back is the right next move: RMMA literature distinguishes single bursts from denser multi-burst episodes, classic and wearable jaw-EMG studies stay event-thresholded and subject-relative, and the new warning against episode-index misuse makes `evt_bursts_per_episode_mean` a better first restore than any raw episode-count family [[bruxism-cap-pass46-bursts-per-episode-add-back-review-2026-05-05]].
- Exact pass46 success/failure read after that review: judge the add-back mainly by whether it gives a targeted `brux2` lift on repaired `A3-only` while preserving the `pass45` `brux1` rescue and keeping the highest control at or below the `0.345` pass45 surface; explicitly reject `evt_episode_count_30s` as the first restore because it is both biologically coarser and literature-criticized as an episode-index shortcut [[bruxism-cap-pass46-bursts-per-episode-add-back-review-2026-05-05]].
- A stricter post-pass46 verdict now closes that lane: the add-back is scientifically coherent and does produce a believable tiny targeted `brux2` lift, but it is still a negative honest-benchmark result because the `0.750 / 0.500 / 1.000` headline stays fixed, `brux2` remains below the highest control (`0.196 < 0.347`), the repaired `A3-only` paired margin slips slightly versus pass45 (`+0.295` -> `+0.292`), and repaired `pass42` still owns the stronger `brux2` separation (`+0.339` versus `-0.151`) [[bruxism-cap-pass46-honest-benchmark-verdict-2026-05-05]].
- Durable next-step read after that stricter verdict: keep `pass45` as the repaired `A3-only` anchor, preserve `pass46` only as a side-variant negative-result memo, and spend the next benchmark card on one new bounded experiment outside this exact one-feature add-back lane rather than stacking more interpretation onto the same tiny shift [[bruxism-cap-pass46-honest-benchmark-verdict-2026-05-05]] [[bruxism-cap-pass45-honest-benchmark-verdict-2026-05-05]].
- A fresh post-pass46 synthesis then fixed that exact next benchmark move: stay on the frozen repaired `pass45` `A3-only` no-shape surface, keep `evt_active_fraction` and `evt_burst_duration_median_s`, drop only `evt_interburst_gap_median_s`, add only `evt_phasic_like_episode_fraction`, and require the same paired subject-surface audit against `pass45` as the primary readout [[bruxism-cap-pass46-next-step-synthesis-2026-05-05]] [[bruxism-cap-campaign-handoff-2026-05-05]].
- Revised campaign-handoff update after the pass46 verdict plus synthesis: the branch should continue narrowly, but the exact failed density add-back lane is now closed. The durable campaign read is to keep `pass45` as the repaired `A3-only` anchor, keep privacy and LLM/RL branch gates closed, preserve `pass46` as a first-class negative side variant, and move next to one fixed-width event-trio swap rather than another one-feature count/density add-back [[bruxism-cap-campaign-handoff-2026-05-05]] [[bruxism-cap-pass46-next-step-synthesis-2026-05-05]].
- Dataset-branch methodology update on 2026-05-06: the repo now has an explicit science-grounded dataset decision rubric for what should happen after the current CAP loop. Positive-label validity, jaw-muscle relevance, and subject-level evaluation honesty should dominate the decision, while bigger raw `N`, GPU/deep-model temptation, or wearable trendiness without open reproducible data should not. Under that rubric the current recommendation is to expand within CAP rather than open a new dataset branch yet, and any future non-CAP branch should first prove stronger label provenance, equally honest subject-held-out evaluation, and real runnable access [[bruxism-cap-dataset-decision-rubric-2026-05-06]].
- Source-audit update on 2026-05-06: public CAP does not expose any honest bruxism-positive expansion beyond `brux1` and `brux2`. Additional CAP work may still expand controls, channels, or within-record stage/event slices, but other pathology groups and CAP-event proxies must not be relabeled as bruxism positives [[bruxism-cap-source-audit-cap-expansion-2026-05-06]].
- Data-strategy synthesis update on 2026-05-06: the broader project now has one explicit next branch choice. CAP is honestly capped on the positive side, but no external dataset is both stronger enough and open enough to replace it immediately, so the next active branch should be one bounded CAP-adjacent control-side expansion rather than either a full stop or a new dataset pivot [[bruxism-cap-next-data-strategy-2026-05-06]].
- Deferred-branch gate update on 2026-05-06: portable jaw-EMG remains the strongest non-CAP backup direction, but it should stay deferred until a candidate cohort proves real access, direct jaw-muscle signal, subject-held-out evaluation support, and an evidence-pack score that clears the explicit pivot gates [[bruxism-cap-next-data-strategy-2026-05-06]].
- Control-side expansion audit update on 2026-05-06: the new bounded `n*` audit turns that branch choice into an exact admissible set. Keep `n3`, `n5`, and `n11` as the verified local control core, admit `n1` and `n2` as the next dual-channel additions, treat `n10` as refresh-needed because the current local EDF is truncated relative to the canonical PhysioNet object, and exclude the remaining healthy controls from this branch because they lack `EMG1-EMG2`, `C4-P4`, or both [[bruxism-cap-control-side-expansion-audit-2026-05-06]].

## Relationship to other tracks
- [[artifact-card-sft]] is paused, not deleted.
- The older tutor adapter remains a negative-result case study.
- The lab's current learning path now treats a reproducible biosignal baseline as the main active experiment.
